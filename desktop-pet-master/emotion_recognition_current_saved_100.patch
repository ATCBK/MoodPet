diff --git a/desktop-pet-master/moodpet/emotion.py b/desktop-pet-master/moodpet/emotion.py
index 206a570..646bbd6 100644
--- a/desktop-pet-master/moodpet/emotion.py
+++ b/desktop-pet-master/moodpet/emotion.py
@@ -26,12 +26,12 @@ FERPLUS_LABELS = [
 FERPLUS_TO_MOODPET = {
     "neutral": "neutral",
     "happy": "happy",
-    "surprise": "surprise",
+    "surprise": "happy",
     "sad": "sad",
-    "angry": "angry",
-    "disgust": "disgust",
-    "fear": "fear",
-    "contempt": "disgust",
+    "angry": "sad",
+    "disgust": "sad",
+    "fear": "sad",
+    "contempt": "sad",
 }
 
 EMOTION_ZH = {
diff --git a/desktop-pet-master/moodpet/emotion_camera.py b/desktop-pet-master/moodpet/emotion_camera.py
index a3c5087..3d67f73 100644
--- a/desktop-pet-master/moodpet/emotion_camera.py
+++ b/desktop-pet-master/moodpet/emotion_camera.py
@@ -35,6 +35,17 @@ def output_to_scores(output: np.ndarray) -> Dict[str, float]:
     }
 
 
+def face_to_blob(face: np.ndarray) -> np.ndarray:
+    face = cv2.resize(face, (64, 64))
+    return cv2.dnn.blobFromImage(
+        face,
+        scalefactor=1.0 / 255.0,
+        size=(64, 64),
+        mean=(0,),
+        swapRB=False,
+    )
+
+
 def ensure_model(model_path: Path) -> None:
     model_path.parent.mkdir(parents=True, exist_ok=True)
     if model_path.exists() and model_path.stat().st_size > 0:
@@ -142,8 +153,7 @@ class EmotionCameraWorker:
 
         x, y, w, h = max(faces, key=lambda face: face[2] * face[3])
         face = gray[y : y + h, x : x + w]
-        face = cv2.resize(face, (64, 64))
-        blob = cv2.dnn.blobFromImage(face, scalefactor=1.0, size=(64, 64), mean=(0,), swapRB=False)
+        blob = face_to_blob(face)
         net.setInput(blob)
         output = net.forward()
         scores = output_to_scores(output)
diff --git a/desktop-pet-master/tests/test_emotion.py b/desktop-pet-master/tests/test_emotion.py
index 054b3f1..c33b4fb 100644
--- a/desktop-pet-master/tests/test_emotion.py
+++ b/desktop-pet-master/tests/test_emotion.py
@@ -33,11 +33,30 @@ class EmotionMappingTest(unittest.TestCase):
         self.assertEqual(state.label_zh, "平静")
         self.assertEqual(state.confidence, 0.65)
 
-    def test_maps_ferplus_contempt_to_chinese_disgust_state(self):
-        state = best_ferplus_emotion({"neutral": 0.1, "contempt": 0.8})
+    def test_ferplus_keeps_clear_happy_expression(self):
+        state = best_ferplus_emotion({"happy": 0.65, "sad": 0.14, "neutral": 0.12})
 
-        self.assertEqual(state.emotion, "disgust")
-        self.assertEqual(state.label_zh, "厌恶")
+        self.assertEqual(state.emotion, "happy")
+
+    def test_ferplus_maps_surprise_to_happy_state(self):
+        state = best_ferplus_emotion({"surprise": 0.7, "neutral": 0.2})
+
+        self.assertEqual(state.emotion, "happy")
+
+    def test_ferplus_maps_negative_expression_to_unhappy_state(self):
+        state = best_ferplus_emotion({"angry": 0.5, "neutral": 0.3})
+
+        self.assertEqual(state.emotion, "sad")
+
+    def test_ferplus_maps_contempt_to_unhappy_state(self):
+        state = best_ferplus_emotion({"contempt": 0.8, "neutral": 0.1})
+
+        self.assertEqual(state.emotion, "sad")
+
+    def test_ferplus_keeps_neutral_when_neutral_leads(self):
+        state = best_ferplus_emotion({"neutral": 0.74, "sad": 0.21, "happy": 0.02})
+
+        self.assertEqual(state.emotion, "neutral")
 
 
 if __name__ == "__main__":
diff --git a/desktop-pet-master/tests/test_emotion_camera.py b/desktop-pet-master/tests/test_emotion_camera.py
index d0675e3..c0d624e 100644
--- a/desktop-pet-master/tests/test_emotion_camera.py
+++ b/desktop-pet-master/tests/test_emotion_camera.py
@@ -2,7 +2,7 @@ import unittest
 
 import numpy as np
 
-from moodpet.emotion_camera import output_to_scores
+from moodpet.emotion_camera import face_to_blob, output_to_scores
 
 
 class EmotionCameraTest(unittest.TestCase):
@@ -14,6 +14,15 @@ class EmotionCameraTest(unittest.TestCase):
         self.assertAlmostEqual(sum(scores.values()), 1.0, places=5)
         self.assertGreater(scores["contempt"], scores["neutral"])
 
+    def test_face_to_blob_normalizes_camera_pixels_for_ferplus(self):
+        face = np.full((64, 64), 255, dtype=np.uint8)
+
+        blob = face_to_blob(face)
+
+        self.assertEqual(blob.shape, (1, 1, 64, 64))
+        self.assertAlmostEqual(float(blob.max()), 1.0, places=5)
+        self.assertAlmostEqual(float(blob.min()), 1.0, places=5)
+
 
 if __name__ == "__main__":
     unittest.main()
